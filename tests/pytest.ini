[pytest]
DJANGO_SETTINGS_MODULE = corp_lis.settings.test
python_files = tests.py test_*.py *_tests.py
addopts = 
    --verbose
    --cov=.
    --cov-report=html
    --cov-report=term
    --ds=corp_lis.settings.test
    --reuse-db
filterwarnings =
    ignore::DeprecationWarning